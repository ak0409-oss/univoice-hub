{
  "name": "univoice-hub",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix univoice-frontend run dev",
    "build": "npm --prefix univoice-frontend run build",
    "build:dev": "npm --prefix univoice-frontend run build:dev",
    "lint": "npm --prefix univoice-frontend run lint",
    "preview": "npm --prefix univoice-frontend run preview",
    "test": "npm --prefix univoice-frontend run test",
    "test:watch": "npm --prefix univoice-frontend run test:watch"
  }
}
